#  Flink stack (ratatoskr up --profile full)
RATATOSKR_PROFILE=minimal
FLINK_REST_PORT=8082
KAFKA_BOOTSTRAP_SERVERS=localhost:9094

# Control API — matches Vite proxy in dashboard/vite.config.ts
RATATOSKR_API_PORT=8090

# Cloudera Manager via CDP Knox (set KNOX_TOKEN in your shell — do not commit)
CM_API_BASE=https://worldwidebank-gateway.go01-dem.ylcu-atmi.cloudera.site/worldwidebank/cdp-proxy-token/cm-api
CM_CLUSTER=worldwidebank
